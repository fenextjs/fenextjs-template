import { LayoutDashboard } from '@/layout/Dashboard';

export const PageProduct = () => {
    return (
        <>
            <LayoutDashboard>Product</LayoutDashboard>
        </>
    );
};

export default PageProduct;
